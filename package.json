{
  "name": "celular-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "start": "cd backend && node index.js"
  },
  "dependencies": {
    "react-router-dom": "^7.9.4"
  }
}
